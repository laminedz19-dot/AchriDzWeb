# نشر AchriDZ على Render أو استضافة Node أخرى

هذا المشروع تطبيق Full-stack مبني على React وVite وExpress وtRPC وDrizzle ORM. يمكن تشغيله على خدمة تدعم Node.js، لكنه يحتاج إلى قاعدة بيانات MySQL أو TiDB، وتخزين ملفات متوافق مع Manus/S3، ومتغيرات بيئة المصادقة والتخزين.

## الملفات المطلوبة

يحتوي أرشيف `achridz-marketplace-web-source.zip` على كود الواجهة والخادم، مخطط Drizzle وملفات migrations، اختبارات Vitest، وملفات `package.json` و`pnpm-lock.yaml` وإعدادات TypeScript وVite. لا يحتوي الأرشيف على `node_modules` أو أسرار البيئة أو السجلات أو ملفات البناء المؤقتة.

## إعدادات Render المقترحة

أنشئ Web Service من مستودع GitHub أو ارفع المشروع إلى مستودع خاص، ثم استخدم الإعدادات التالية:

| الإعداد | القيمة |
|---|---|
| Runtime | Node |
| Build Command | `corepack enable && pnpm install --frozen-lockfile && pnpm build` |
| Start Command | `pnpm start` |
| Health Check | `/` |
|

لا تثبّت المنفذ يدويًا؛ الخادم يقرأ المنفذ الذي توفره المنصة من متغير البيئة `PORT`.

## متغيرات البيئة

أضف القيم الحقيقية من لوحة الاستضافة، ولا تضعها داخل Git أو ملف ZIP:

| المتغير | الغرض |
|---|---|
| `DATABASE_URL` | رابط اتصال MySQL/TiDB وقاعدة البيانات الخاصة بالمشروع. |
| `JWT_SECRET` | توقيع جلسات المستخدمين؛ يجب أن يكون سرًا عشوائيًا طويلًا. |
| `VITE_APP_ID` | معرّف تطبيق Manus OAuth. |
| `VITE_OAUTH_PORTAL_URL` | بوابة تسجيل الدخول OAuth. |
| `OAUTH_SERVER_URL` | خادم OAuth الخلفي. |
| `OWNER_OPEN_ID` و`OWNER_NAME` | بيانات المالك المستخدمة في نظام المشروع. |
| `BUILT_IN_FORGE_API_URL` و`BUILT_IN_FORGE_API_KEY` | خدمات Manus المدمجة، ومنها التخزين. |
| `VITE_FRONTEND_FORGE_API_URL` و`VITE_FRONTEND_FORGE_API_KEY` | إعدادات خدمات Manus التي تصل إليها الواجهة. |

قد تحتاج إلى متغيرات التحليلات الموجودة في إعدادات المشروع إذا أردت الإبقاء على التحليلات.

## قاعدة البيانات

قبل تشغيل التطبيق على الاستضافة الجديدة، أنشئ قاعدة MySQL أو TiDB فارغة، ثم طبّق ملفات migrations بترتيبها. لا تحذف قاعدة البيانات الحالية دون نسخة احتياطية. بعد ضبط `DATABASE_URL`، يمكن تنفيذ:

```bash
pnpm db:push
```

ويجب مراجعة SQL الناتج قبل تطبيقه على قاعدة تحتوي بيانات حقيقية.

## تسجيل الدخول OAuth

يجب إضافة عنوان الاستضافة الجديدة إلى قائمة Redirect/Callback URLs في إعداد Manus OAuth. المسار المستخدم في المشروع هو:

```text
https://YOUR-DOMAIN/api/oauth/callback
```

إذا لم تتم إضافة العنوان، سيعمل الموقع العام لكن سيفشل تسجيل الدخول ونشر الإعلانات ولوحة الإدارة.

## التخزين ورفع الصور

رفع صور الإعلانات ووصول الدفع يعتمد على تخزين S3/Manus. يجب توفير إعدادات التخزين الصحيحة للخادم الجديد أو الإبقاء على المشروع مستضافًا في Manus. لا تضع الصور المرفوعة داخل مستودع Git أو داخل الأرشيف؛ الصور يرفعها المستخدمون أثناء تشغيل الموقع.

## حساب الإدارة

المصادقة تتم عبر Manus OAuth وليس عبر كلمة مرور ثابتة داخل الكود. البريد المصرح به للإدارة هو:

```text
laminedz.19@gmail.com
```

بعد نجاح تسجيل الدخول بهذا الحساب، افتح `/admin`. يجب عدم مشاركة أسرار OAuth أو `JWT_SECRET` أو بيانات قاعدة البيانات.

## ملاحظة مهمة

الاستضافة الثابتة مثل GitHub Pages لا تشغل هذا المشروع وحدها لأنها لا توفر Express أو tRPC أو قاعدة بيانات. Render أو Railway أو Manus Hosting أنسب من الاستضافة الثابتة، مع مراجعة شروط الخطة المجانية وحدود النوم والاستخدام قبل الاعتماد عليها في الإنتاج.
